import type { ReactElement } from 'react';

// Placeholder only. There is no Figma reference for this screen at all yet, despite the engine
// already modeling 4 distinct EngineStatus values it needs to cover - see the UI spec's punch list.
export function PermissionScreen(): ReactElement {
  return <div>Permission (stub)</div>;
}
