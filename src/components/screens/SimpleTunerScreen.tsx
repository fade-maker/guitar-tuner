import type { ReactElement } from 'react';

// Placeholder only - no layout, no styling, no Figma. Real implementation lands once the UI spec's
// blocking Figma gaps (see CLAUDE.md) are closed.
export function SimpleTunerScreen(): ReactElement {
  return <div>Simple Tuner (stub)</div>;
}
