import type { ReactElement } from 'react';

// Placeholder only. The Figma screen itself is still a work in progress - do not build against it
// yet (see CLAUDE.md / UI spec).
export function SelectTuningScreen(): ReactElement {
  return <div>Select Tuning (stub)</div>;
}
