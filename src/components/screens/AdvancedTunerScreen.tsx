import type { ReactElement } from 'react';

// Placeholder only - no layout, no styling, no Gauge, no Figma. Real implementation is blocked on
// the gauge behaviour and PitchStatusBadge in-context states (see CLAUDE.md / UI spec punch list).
export function AdvancedTunerScreen(): ReactElement {
  return <div>Advanced Tuner (stub)</div>;
}
